def check_schedule_conflict(itinerary: str) -> bool:
    """
    Dummy schedule conflict checker.

    Args:
        itinerary (str): Planned itinerary text.

    Returns:
        bool: False if no conflict.
    """
    # For now we assume no conflict.
    return False
